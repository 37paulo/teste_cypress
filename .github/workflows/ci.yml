name: tests-ci
on: [push]
jobs:
  cypress-electron-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5.8.2
      
      - name: Install dependencies
        run: npm install

      - name: Run E2E tests
        run: npx cypress run
